# Test-only dependencies for the s3export graph-projection Lambda.
# Install with:  pip install -r requirements-dev.txt
#
# moto is intentionally NOT used. The export helper creates its boto3
# DynamoDB resource and S3 client inside each call, so the tests replace the
# module's `boto3` with plain in-memory fakes that record the CSV bodies
# handed to `s3.put_object`. This gives precise, dependency-free control over
# scanned items, dedup behaviour, and the empty-table has-items guard.
pytest>=8.0
boto3>=1.43.69   # keeps parity with the collection Lambda's test deps
