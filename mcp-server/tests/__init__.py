"""Test package for the ARIA-gv MCP server.

Present so that ``python -m unittest tests.test_queries`` (run from within the
``mcp-server/`` directory) resolves ``tests`` as an importable package, in
addition to ``python -m unittest discover -s tests`` / ``-s mcp-server/tests``.
"""
